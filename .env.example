# Example env for the whole project (fill per-service)
# DynamoDB table name used by many services
TABLE_NAME=Orders
# For pedidos-api
SQS_QUEUE=pedidos-api-dev-pedidos-queue
# For workflow
EVENT_BUS=orders-bus
STEP_FUNCTION_ARN=arn:aws:states:us-east-1:282163831899:stateMachine:KfcOrderWorkflow
# You can also leave this blank and let `pedidos-backend` read the Step Function ARN from
# the CloudFormation export created by `kfc-workflow` (recommended when deployed together).
# For frontend (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3000
# For integrations service
EVENT_BUS=orders-bus

# NOTE: replace ARNs, region and names with your AWS account values
